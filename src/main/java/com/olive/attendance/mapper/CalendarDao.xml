<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.olive.attendance.dao.CalendarDao">
	 <insert id="insert" parameterType="com.olive.dto.Calendar">	 
	 	INSERT INTO Calendar(STARTDATE, ENDDATE, CONTEXT) 
	 	VALUES(#{startdate}, #{enddate}, #{context})
	 </insert>
	 <select id="getcalendarList" resultType="com.olive.dto.Calendar">
	 SELECT title, startdate as start, enddate as end, color FROM Calendar
	 </select>
	 <select id="getattList" resultType="com.olive.dto.Att_Record">
	 select a.attname as title , ar.starttime as start, ar.endtime as end,color FROM Att_Record ar join ATTENDANCE a ON ar.ATTCODE =a.ATTCODE ;
	 </select>
	 
</mapper>